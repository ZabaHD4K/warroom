services:
  db:
    image: postgis/postgis:16-3.4
    environment:
      POSTGRES_DB: warroom
      POSTGRES_USER: warroom
      POSTGRES_PASSWORD: warroom
    ports:
      - "5432:5432"
    volumes:
      - warroom_data:/var/lib/postgresql/data

volumes:
  warroom_data:
